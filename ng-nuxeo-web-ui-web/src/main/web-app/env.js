(function (window) {
    window.__env = window.__env || {};
  
    window.__env.nuxeoUrl = `${location.origin}/nuxeo`;
    window.__env.webAppUrl = 'http://localhost:8080/nuxeo/web-app';
    window.__env.authClientId = 'ng-nuxeo-web-app';
  }(this));
  